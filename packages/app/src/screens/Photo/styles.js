import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  detection: {
    position: 'absolute',
    borderColor: 'white',
    borderWidth: 3,
    borderRadius: 5,
  },
});
export default styles;
